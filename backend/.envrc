# direnv 配置文件（可选）
# 如果安装了 direnv，会自动激活虚拟环境
# 安装: brew install direnv (macOS) 或 apt-get install direnv (Linux)

# 激活虚拟环境
source venv/bin/activate

# 设置 PYTHONPATH
export PYTHONPATH="${PWD}:${PYTHONPATH}"

